
import wx

class App > wx.wxApp
  def OnInit()
    frame = wx.wxFrame.new(nil, -1, "foo")
    self.SetTopWindow(frame)
    frame.Show(true)
  end
end

app = App.new()
app.OnRun()

# vim: tabstop=2 shiftwidth=2 expandtab softtabstop=2
