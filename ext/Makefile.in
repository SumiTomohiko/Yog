# Makefile.in generated by automake 1.10.1 from Makefile.am.

DEPDIR = .deps
SO_EXT = .so
am__include = include

include ./$(DEPDIR)/concurrent.Po

all: concurrent$(SO_EXT)

concurrent$(SO_EXT): concurrent.c
	$(CC) -I../include -shared -O2 -Wall -lpthread -o $@ $<

clean: 
	rm -f *$(SO_EXT)

distclean: 
	rm -f *$(SO_EXT)
	rm -f Makefile

maintainer-clean: 
	rm -f *$(SO_EXT)
	rm -f Makefile
