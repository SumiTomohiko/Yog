
SO_EXT = .so

all: concurrent$(SO_EXT)

concurrent$(SO_EXT): concurrent.c
	$(CC) -I../include -shared -O2 -Wall -lpthread -o $@ $<

clean: 
	rm -f *$(SO_EXT)

distclean: 
	rm -f *$(SO_EXT)
	rm -f Makefile

maintainer-clean: 
	rm -f *$(SO_EXT)
	rm -f Makefile
